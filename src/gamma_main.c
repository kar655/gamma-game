
#include "inputParser/parser.h"

#include <stdio.h>
#include <stdbool.h>

int main() {

    // 0 - keep reading, 1 - batch, 2 - interactive
//    int mode = 0;
//
//    while (mode == 0) {
//        mode = gameSuccess();
//
////        if (mode != 0)
////            break;
//
//        fflush(stdout);
//    }

//    playMode(mode);
//
//    clearMemory();


    playGame();

    return 0;
}